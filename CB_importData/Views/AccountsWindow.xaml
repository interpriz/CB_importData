<Window x:Class="CB_importData.Views.AccountsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CB_importData.Views"
        mc:Ignorable="d"
        Title="AccountsWindow" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="CBGrid" AutoGenerateColumns="true" ItemsSource = "{Binding }">
            <DataGrid.Columns>
                <DataGridTextColumn Header="BIC" Binding="{Binding Path=bICDirectoryEntry.BIC}" Width="90" />

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
